# AIWars minigame manifest — the ONE file you edit per game (besides the game code).
# Read at referee boot by the aiwars-minigame library and published to the platform
# (s3://<deployment>-recorded-views/games/<name>/game.toml) so the site knows, pre-world,
# whether this game offers demo (human play).
[game]
name = "wipeout"                   # the game id — MUST equal the S3 publish folder + the game key
bin = "aiwars-mcp-wipeout"         # the cargo package/bin the referee image builds
category = "turn-based"            # turn-based | freeform
title = "Wipeout Gauntlet"
description = "Wipeout Gauntlet — an AIWars minigame refereed by the aiwars-mcp-warden custom core."

# Demo (epic #317): this satellite has no seat view yet, so human play is OFF — the referee
# refuses seats and the site hides the demo button. Flip to `true` once it ships a seat view.
[demo]
enabled = false

# Site media, owned by THIS repo (the platform carries no game art, #400): repo-relative
# paths the image-publish rail uploads next to the manifest (s3://…-recorded-views/games/
# <name>/thumb), served by the view edge and preferred by the site over any baked-in
# fallback. Seeded with the previously-baked recorded-match GIF; regenerate from this
# game's own view when the art changes (see aiwars-werewolf for the harness pattern).
[media]
thumbnail = "media/thumb.gif"
cover = "media/cover.png"
